============
Installation
============

At the command line::

    $ easy_install orange_ipfe

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv orange_ipfe
    $ pip install orange_ipfe
